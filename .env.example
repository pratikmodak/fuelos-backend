# ═══════════════════════════════════════════════
# FuelOS Backend — Environment Variables
# Copy this to .env for local dev
# On Render: add these in Dashboard → Environment
# ═══════════════════════════════════════════════

# Database (Render PostgreSQL or Supabase)
DATABASE_URL=postgresql://user:password@host:5432/fuelos

# JWT secrets — generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_min_64_chars
ADMIN_JWT_SECRET=your_admin_jwt_secret_here_min_64_chars

# SuperAdmin password (first login, then create other admins via dashboard)
SUPERADMIN_EMAIL=superadmin@yourcompany.com
SUPERADMIN_PASSWORD=your_superadmin_password_here

# Frontend URL (for CORS)
FRONTEND_URL=https://your-app.vercel.app

# Razorpay (optional — for real payments)
RAZORPAY_KEY_ID=rzp_test_xxxx
RAZORPAY_KEY_SECRET=your_razorpay_secret

# Email OTP (optional — uses Gmail SMTP)
EMAIL_USER=your@gmail.com
EMAIL_PASS=your_gmail_app_password

# WhatsApp (optional — Gupshup or similar)
WA_API_KEY=your_whatsapp_api_key
WA_SOURCE=91xxxxxxxxxx

# Server
PORT=3000
NODE_ENV=production
